# Soluciones MMIV

Landing page de **Proyectos y Soluciones Inmobiliarias MMIV** preparada para desplegarse en Railway.

## Ejecutar localmente

```bash
npm install
npm start
```

Railway detecta `package.json` y ejecuta `npm start`. El servidor usa automáticamente la variable `PORT` provista por Railway.
